/*
need to know functions().
void formHttpHeaders(struct httpHeaders &specs, std::string &httpHeaders);
bool fileDiskToMemory(const char *filePath, struct metabuf &buf);
and the http structs and enums and metabuf

*/

#include "sendIndexPage.cpp"
#include "sendFavicon.cpp"
