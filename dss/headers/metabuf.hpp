struct metabuf
{
    long length;
    void *start;
};

/*
    metabuf is a struct which contains a pointer to where a buffer is stored in memory,
    and stores the length of the buffer.
*/
