bool fileDiskToMemory(const char *filePath, struct metabuf &buf, int max);
bool fileDiskToMemory(const char *filePath, struct metabuf &buf);

/*
    fileDiskToMemory() will load a file located at filePath, into
    the metabuf buffer buf.
*/
